<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="Lecture.Evaluation.dao.UserDao">

  <resultMap type="Lecture.Evaluation.user.UserDTO" id="UserMap">
    <id column="userID"   property="userID"/>
    <result column="userPassword"  property="userPassword"/>
    <result column="userEmail"    property="userEmail"/>
    <result column="userEmailHash" property="userEmailHash"/>
    <result column="userEmailChecked" property="userEmailChecked"/>
   
    
  </resultMap>
  
  <select id="login" resultMap="int">
   SELECT
    	userPassword 
    FROM
     	USER  
    
   

    where 
		 userID =#{userID}
					
   
  </select>
  
  <insert id="join" parameterType="UserDTO">
    INSERT INTO USER (userID ,
    	userPassword,
    	userEmail,
    	userEmailHash,
    	userEmailChecked
    
   	 ) 
    values(#{userID},   #{userPassword},
   	#{professorName},
   	#{userEmail},
   	 #{userEmailHash},
   	0
   
   	
  </insert>
  
  <select id="findById" resultMap="UserMap" parameterType="string">
    SELECT userID
     FROM USER 
     WHERE userID =#{UserID}
    
   
  </select>
  
  <select id="findByIdAndPassword" resultMap="UserMap" parameterType="map">
    SELECT userID,userPassword
     FROM USER 
     WHERE userID =#{UserID} and userPassword=userPassword(#{userPassword})
    
   
  </select>
   <select id="getUserEmailChecked" resultMap="boolean" parameterType="string">
   SELECT userEmailChecked 
   FROM USER 
   WHERE userID = #{userID}
   
  </select>
   <select id="getUserEmail" resultMap="String" parameterType="string">
	SELECT userEmail 
	FROM USER 
	WHERE userID = #{userID}
   
  </select>
  <update id="setUserEmailChecked" parameterType="string">
   UPDATE USER 
   SET userEmailChecked = true 
   WHERE userID =#{userID}
  </update>
  
 
  <!-- 회원 인증키(난수) 생성 -->
	<insert id="alterKey" parameterType="map">
		UPDATE USER SET userKey = #{userKey} WHERE userID = #{userID}
	</insert>
	<!-- 회원 인증키(난수) Y로 변경 -->
	<update id="alterUserKey" parameterType="map">
		UPDATE USER SET userKey = 'Y' WHERE userID = #{userID} and userKey = #{userKey}
	</update>

</mapper>
